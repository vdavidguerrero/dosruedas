<div class="container">
  <h2>Login</h2>

  <div class="form-horizontal">
      <%= form_tag(action: 'authenticate') do %>

          <div class="form-group ">
            <%= label(:email, "Correo", class:"control-label col-sm-1") %>
            <div class="col-sm-3">
              <%= text_field_tag(:email) %>
            </div>
          </div>

          <div class="form-group ">
            <%= label(:password, "Contraseña", class:"control-label col-sm-1") %>
            <div class="col-sm-3">
              <%= password_field_tag(:password) %>
            </div>
          </div>

            <%= "#{flash[:message]}" %>
          <div class="form-buttons">
            <%= submit_tag("Create Section") %>
          </div>

      <% end %>
  </div>
</div>
